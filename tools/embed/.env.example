#############################################
# MODEL PROVIDER CONFIGURATION
#############################################

# Defines the AI model provider to use.
# Options:
# - "ollama" (default): Uses the Ollama API.
# - "hf": Uses Hugging Face models (requires an API key).
PROVIDER=ollama

#############################################
# OLLAMA CONFIGURATION
#############################################

# Base URL for the Ollama API service.
OLLAMA_URL=http://ollama:11434

#############################################
# HUGGING FACE CONFIGURATION
#############################################

# API key required to access Hugging Face models.
HF_API_KEY=your-huggingface-api-key

#############################################
# ELASTICSEARCH CONFIGURATION
#############################################

# Base URL for the Elasticsearch service.
ES_URL=http://elasticsearch:9200

# Default index for storing and retrieving vectorized data.
ES_INDEX=default

# Authentication credentials for Elasticsearch (if required).
ES_BASIC_AUTH_USERNAME=
ES_BASIC_AUTH_PASSWORD=

#############################################
# EMBEDDING MODEL CONFIGURATION
#############################################

# Embedding model used for vector representations.
EMBEDDING_MODEL_NAME=snowflake-arctic-embed2

# Embedding model used when Hugging Face is selected as the provider.
HF_EMBEDDING_MODEL_NAME=sentence-transformers/all-mpnet-base-v2

#############################################
# API CONFIGURATION
#############################################

# Base URL for the API service.
API_URL=http://api:8000

# API key for authentication (if required).
API_KEY=EXAMPLE_API_KEY

#############################################
# SERVER CONFIGURATION
#############################################

# Defines the host on which the server runs.
# "0.0.0.0" allows access from any network interface.
HOST=0.0.0.0

# Port number the server listens on.
PORT=5000

# Enables or disables debug mode.
# Set to "true" for verbose error logging and live reload (not recommended for production).
DEBUG=false

#############################################
# ASSET CONFIGURATION
#############################################

# Base URL for serving static assets.
ASSET_URL=/static

# Cache timeout for static assets (in seconds).
# 31536000 seconds = 1 year (recommended for long-term caching).
ASSET_CACHE_TIMEOUT=31536000

# Enables debug mode for assets.
# When "true", assets will not be cached and will be served in development mode.
ASSET_DEBUG=false
